详细操作步骤
1. 图片读取：读取本地目标图片文件（需确保图片路径正确，支持常见格式如.jpg、.png等）；
2. 灰度转换：将读取的彩色图片转换为灰度图，简化图像色彩信息；
3. 单颜色输出：将灰度图以单一颜色（自定义指定颜色）呈现，统一图像视觉效果；
4. 图像旋转：通过生成旋转矩阵、设置旋转中心与角度，对单颜色图像执行旋转操作，确保旋转后图像完整无截断；
5. 边缘描边：为旋转后的图像添加边缘描边效果，突出图像轮廓特征。
三、关键技术依赖
- 基于 OpenCV 库实现图像读取、灰度转换、旋转及描边核心功能；
- 依赖 NumPy 库辅助处理图像数组（如旋转矩阵计算、尺寸适配等）。
